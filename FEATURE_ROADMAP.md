# Baton 特性跟踪与推进路线

> **用途：** 本文记录 Baton 在不引入公共 Baton 后端、且不实施远程推送的前提下，下一阶段值得推进的产品特性。它是维护者的交接与跟进清单，不替代对外协议 [`BATON_SPEC.md`](BATON_SPEC.md) 或内部约定 [`AGENTS.md`](AGENTS.md)。
>
> **更新方式：** 开始实施时，将相应特性状态改为“进行中”，补充负责人、关联 PR/issue 与决策记录；完成后填写验收证据和实际发布版本。协议或安全边界发生变化时，必须同步更新 `BATON_SPEC.md`、`JAVA_INTEGRATION.md` 与相关测试/fixture。

## 产品基线与决策

Baton 是服务端 Conversation 的 iOS Companion：服务端是唯一事实源，iPhone 与 Web 是平级客户端。它的优势是让用户在离开电脑时，能以语音或少量文本自然地补充上下文、继续追问和恢复已有对话；它不是完整业务工作台，也不应成为消息中继。

本路线已确认的约束：

- **不做远程推送。** 通用 Baton App 若让每个接入服务直接向 APNs 推送，将涉及 Apple 私钥的安全分发；在没有受控 Push Gateway 的前提下，不纳入本阶段。
- **不引入 Baton 公共后端。** 每个接入服务继续持有 Conversation、权限、消息与事件日志。
- **仍不做**图片/相机/文件上传、Agent action approval、Tool UI、生成式业务界面、浏览器镜像。
- 只接受服务端声明的同源 HTTP(S) endpoint；HTTP 在 UI 中持续标记为未加密。
- token、`device_proof` 和 outbox 只能保存在 Keychain；不得写入日志、UserDefaults、App Group 文件或普通磁盘缓存。

## 路线总览

| 阶段 | 特性 | 价值 | 服务端/协议改动 | 初始状态 |
| --- | --- | --- | --- | --- |
| 1 | 语音输入打磨 | 强化 Baton 的核心差异化输入 | 否 | 未开始 |
| 1 | 待发送箱与离线恢复可视化 | 让网络波动下的发送可信 | 否 | 首版已实现，待真机/故障验证 |
| 2 | 多会话收件箱与本地已读状态 | 降低多 Conversation 切换成本 | 否（可选增强除外） | 未开始 |
| 3 | 文本分享菜单与引用文本 | 把外部上下文自然带入对话 | 否 | 未开始 |
| 4 | 快捷短语与恢复反馈 | 提升高频短追问和异常可理解性 | 否 | 未开始 |
| 候选 | 服务端声明的快捷动作 | 支持服务特定的轻量提示 | 是，小型 discovery 扩展 | 暂缓 |
| 候选 | 服务端托管的只读文档引用 | 展示受控的补充材料 | 是，媒体/文档协议扩展 | 暂缓 |

## 阶段 1：核心输入与可靠性

### F-01：语音输入打磨

- **状态：** 未开始
- **目标用户场景：** 用户步行、短暂离开电脑或戴着耳机时，想快速补充一段背景、提出追问，且仍能在发送前确认文字。
- **产品范围：**
  - 按住说话与清晰的录音/识别中状态；
  - 静音自动停止的可配置策略；
  - 转写中的增量文本、取消、重试与撤销；
  - 未发送转写草稿的恢复；
  - 蓝牙耳机输入和语言切换的体验验证。
- **明确不做：** 不上传音频；不自动发送转写；不把语音识别结果作为服务端特殊消息类型。
- **实现边界：** 继续由 `Features/Speech/SpeechInputService.swift` 负责本地听写，`BatonViewModel` 仅接收用户确认后的普通文本。
- **验收标准：**
  - 用户可在任一活跃会话中开始、取消并重试听写；
  - 未确认内容绝不发送到服务端；
  - 无网络时，确认后的文本进入既有 outbox，而非丢失；
  - Speech 权限拒绝、识别不可用和中断均有可操作的 UI 提示；
  - 覆盖 locale resolver 与新增交互状态的单元测试。
- **风险/待决项：** 自动停止阈值须经真机和不同语言验证，避免过早截断。

### F-02：待发送箱与离线恢复可视化

- **状态：** 首版已实现，待真机/网络故障验证
- **目标用户场景：** 用户在电梯、地铁或切换网络时发送文本，需要清楚知道消息是否已被服务端确认，而不是猜测是否应重发。
- **现有基础：** `KeychainStore` 已保存 `PendingOutboxMessage`；`BatonViewModel` 已具备重试和幂等发送基础。
- **产品范围：**
  - 显示发送中、等待发送、重试中、需用户处理等状态；已确认后从待发送箱移除；
  - 允许用户查看、以原始 `client_message_id` 显式重试或放弃未送达文本；
  - 在会话恢复后自动继续安全重试，并说明结果；
  - 保持 `client_message_id` 的幂等语义，避免重复消息。
- **明确不做：** 不把消息持久化到 Keychain 之外；不承诺系统被强制终止期间的即时后台投递；不支持编辑后重发。网络超时后服务端是否已接收原请求无法确定，修改文本或 UUID 都可能造成重复语义。
- **验收标准：**
  - 网络错误、进程重启和服务端超时后，待发送文本可恢复；
  - 同一 outbox 项多次重试在服务端只产生一条消息；
  - 用户放弃后，相关 Keychain outbox 项被安全移除；
  - 401、会话结束与不可恢复错误有不同且明确的处理路径；
  - 新增 outbox 状态机测试。
- **风险/待决项：** 需定义“可自动重试”与“必须用户确认”的错误分类，不能无界重试。

## 阶段 2：多会话恢复

### F-03：多会话收件箱与本地已读状态

- **状态：** 未开始
- **目标用户场景：** 用户已加入项目、客户、个人研究等多段 Conversation，打开 Baton 时希望快速判断应回到哪一段。
- **现有基础：** App 已保存多个会话并可进行 availability probe；每段 Conversation 有 snapshot event cursor。
- **产品范围：**
  - 按最近打开/最近活动排序，支持置顶和本地标签；
  - 显示会话标题、服务名、最近活动时间、连接可用性和运行中状态；
  - 保存每个会话本地“最后已读 cursor”，在打开 App 或手动刷新时标注有更新；
  - 明确区分“有新事件”“连接不可用”“凭据已失效”“会话已结束”。
- **明确不做：** 没有推送时，不承诺后台实时未读；不将远端消息正文持久化到普通本地存储。
- **实现边界：** 本地元数据可进 Keychain 中既有 session index；会话事实仍通过认证 snapshot/SSE 获取。
- **验收标准：**
  - 至少三个已保存会话可稳定排序、置顶、切换；
  - 每次打开或刷新后，已读标记只依据已验证的 cursor 变化；
  - 撤销或结束一个会话不影响其他会话；
  - availability probe 不会意外建立 SSE 或启动 Agent run；
  - 覆盖索引去重、排序、已读 cursor 和撤销隔离的测试。
- **风险/待决项：** “最近活动”若需跨设备准确排序，可能需要协议增加服务端时间字段；首版可使用本地最后成功同步时间并明确其含义。

## 阶段 3：外部文本上下文入口

### F-04：iOS 文本分享菜单与引用文本

- **状态：** 未开始
- **目标用户场景：** 用户在 Safari、邮件、备忘录或企业 IM 中发现一段内容，想把它带入某个已加入的 Conversation 继续讨论。
- **产品范围：**
  - 新建 Share Extension，只接收纯文本；
  - 选择一个已配对会话，进入可编辑的撰写体验；
  - 可选引用样式和可编辑的来源名称；
  - 字符数限制、隐私说明和显式“发送”确认；
  - 最终仍以现有普通文本/Markdown 消息发送。
- **明确不做：** 不接收图片、文件、网页自动抓取、Cookie、登录态或任意 URL 的后台读取；不在用户未确认时发送。
- **安全实现要求：**
  - Share Extension 不把 credential、proof 或 outbox 写入 App Group 文件；如需访问会话凭据，使用同一 Keychain access group，并最小化读取范围；
  - 不记录分享原文到日志；
  - 分享内容在扩展和主 App 间传递时避免普通磁盘落盘。
- **验收标准：**
  - 系统分享菜单仅在纯文本兼容时显示 Baton；
  - 用户能取消、选择会话、编辑，并在确认后仅发送一次；
  - 未配对或会话凭据失效时提供安全的恢复路径，不泄露分享文本；
  - 超长文本、特殊 Unicode 与 Markdown 字符得到可预期处理；
  - 使用真机验证 extension 生命周期与 Keychain access group 行为。
- **风险/待决项：** 扩展的短生命周期及 UI 能力有限；应优先交给主 App 完成会话选择/发送，扩展只安全地发起 handoff。

## 阶段 4：高频操作与可解释恢复

### F-05：快捷短语

- **状态：** 未开始
- **目标用户场景：** 移动端用户高频发送“继续”“总结当前结论”“列出风险”“下一步建议”等短追问。
- **产品范围：**
  - 本地可配置短语列表与排序；
  - 点击后填入 composer，不直接发送；
  - 提供少量可本地化的默认短语。
- **明确不做：** 不执行服务端 Tool；不以按钮绕过用户审阅；不预设业务领域语义。
- **验收标准：**
  - 短语可以新增、编辑、删除、排序；
  - 所有短语均先进入可编辑 composer；
  - 不影响语音转写、outbox 或消息幂等性。
- **风险/待决项：** 本地配置的安全存储位置需与“凭据只能放 Keychain”的约束区分；短语不含机密时可使用普通应用设置，含敏感默认内容则不应预置。

### F-06：会话恢复反馈

- **状态：** 未开始
- **目标用户场景：** 服务端重启、SSE cursor 过期、token 被撤销或共享会话被结束时，用户需要理解发生了什么并知道下一步。
- **现有基础：** API client、reducer 和 view model 已处理 snapshot、SSE resync、401 与 `conversation_closed`。
- **产品范围：**
  - 以用户语言显示“正在恢复”“已同步至最新状态”“此设备授权已失效”“该会话已结束”；
  - 每种终态提供一个主操作，例如重新连接、重新扫码或返回会话列表；
  - 将技术错误码留在诊断层，不直接呈现给用户。
- **验收标准：**
  - resync 会重新获得原子 snapshot，且 UI 不出现重复/缺失的本地状态；
  - 401 仅移除匹配凭据并回到安全配对流程；
  - 结束会话后，所有本地关联状态被清理且其他会话继续可用；
  - 覆盖 resync、credential invalidation 和 conversation closed 的 reducer/view model 测试。

## 暂缓候选

### C-01：服务端声明的快捷动作

服务可在 discovery document 中声明建议追问，App 仅把它填入 composer。该功能能让服务提供领域化提示，但需要扩展 V1.1 discovery 与 Java/mock fixture 的契约；在本地快捷短语验证价值前暂缓。

### C-02：服务端托管的只读文档引用

可在已有“认证、同源、受限内存缓存”的图片模型上，设计只读文档卡片或预览。它仍不能成为文件上传通道。因 MIME、渲染、安全扫描和缓存策略复杂度明显上升，在文本分享的真实使用数据明确后再评估。

## 交付与验收门槛

每个特性合并前至少满足：

1. `git diff --check` 通过；
2. iOS Debug build 通过；涉及网络配置时也运行 Release build；
3. 修改 reducer、配对、网络或持久化时运行 `BatonTests`；
4. 修改协议或 mock fixture 时同步更新 `BATON_SPEC.md`、`JAVA_INTEGRATION.md`、`mock_server/smoke_test.py`，并执行 smoke test；
5. 真机验证涉及相机、Speech、Share Extension、Keychain access group 或系统权限的功能；
6. 不在代码、日志、测试 fixture 或文档示例中写入 token、`device_proof`、Cookie、APNs 私钥或真实用户内容。

## 推荐推进顺序与决策点

1. **F-01 + F-02：** 先提升语音输入与发送可靠性。它们完全在客户端现有边界内，风险最低，也最直接兑现 Baton 的核心价值。
2. **F-03：** 接着完善多会话恢复。完成后再观察用户是否真的需要跨会话的“未读”认知，而不是假设推送存在。
3. **F-04：** 再建设文本分享入口。开始前先完成 Share Extension 的 Keychain access group 技术验证；若无法保证敏感数据不落盘，停止实施并重新设计 handoff。
4. **F-05 + F-06：** 最后补齐高频快捷操作与异常可解释性，打磨日常使用闭环。
5. **评估 C-01 / C-02：** 仅在上述特性发布并获得实际使用反馈后决定是否扩协议。任何协议扩展都必须维持“普通文本、受控只读内容、服务端事实源”的边界。

## 当前交接状态

- 文档创建日期：2026-09-01
- 当前阶段：F-02 首版已实现；F-01、F-03 至 F-06 尚未开始
- 已排除项：远程推送 / APNs 直连方案
- 最近验证：iOS `Baton` Debug Simulator build、`BatonTests` 与 `git diff --check` 已通过；仍需在真机以断网、恢复连接、放弃重试和会话切换场景验证 F-02。
